<div>
    <h1>{{user.first}} {{user.last}}</h1>

<table class="userInfoTable">
    <tr>
              <td>Address: </td>
              <td>{{user.address}}</td>
    </tr>
    <tr>
              <td>Phone Number: </td>
              <td>{{user.phone}}</td>
    </tr>
    
    <tr>
        <td><form action="/users"><input type="Submit" value="Back to Users" /></form></td>
        <td><form action="/users/{{user.id}}/pizza/new"><input type="submit" value="New Order" /></form></td>
        <td></td>
    </tr>
    <tr>
        <td><form action="/users/{{user.id}}/edit"><input type="submit" value="Edit Profile" /></form></td>
        <td><form action="/users/{{user._id}}/delete"><input type="submit" value="Delete Profile"></form></td>
    </tr>
</table>
</div>